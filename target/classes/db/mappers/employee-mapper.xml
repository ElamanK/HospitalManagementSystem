<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "org/apache/ibatis/builder/xml/mybatis-3-mapper.dtd">
<mapper namespace="database.bankdb.dao.daointerfaces.IEmployeeDAO">
    <resultMap id="employeeResultMap" type="Employee" autoMapping="false">
        <id column="employee_id" property="id"/>
        <result column="firstname" property="firstname"/>
        <result column="lastname" property="lastname"/>
    </resultMap>
    <select id="getEntityById" parameterType="int" resultMap="employeeResultMap">
        SELECT * FROM employees
        WHERE employee_id = #{id};
    </select>
    <insert id="insertEntity" keyProperty="employee_id">
        INSERT INTO employees (firstname, lastname)
        VALUES (#{firstname}, #{lastname});
    </insert>
    <select id="getAllEntities" resultMap="employeeResultMap">
        SELECT * FROM employees;
    </select>
    <delete id = "removeEntity" parameterType = "int">
        DELETE from employees WHERE employee_id = #{id};
    </delete>
    <update id="updateEntity" parameterType = "Employee">
        UPDATE employees SET
        firstname = #{firstname},
        lastname = #{lastname}
        WHERE employee_id = #{id};
    </update>
</mapper>